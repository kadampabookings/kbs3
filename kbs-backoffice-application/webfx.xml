<?xml version="1.0" encoding="UTF-8"?>
<project applicationId="org.kadampabookings.kbs.backoffice" applicationLabel="KBS" pwa="true">

    <description>The KBS Back-Office application (cross-platform non-executable module).</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>

        <plugin-module>kbs-client-conf-plugin</plugin-module>
        <plugin-module executable-target="gluon">kbs-client-conf-gluon-plugin</plugin-module>

        <!-- Base activities -->
        <plugin-module>modality-base-backoffice-max-plugin</plugin-module>

        <!-- CRM activities -->
        <plugin-module>modality-crm-backoffice-max-plugin</plugin-module>
        <!-- Was designed for both clients, but actually used only in the back-office for now -->
        <plugin-module>modality-crm-client-profile-plugin</plugin-module>

        <!-- Ecommerce activities -->
        <plugin-module>modality-ecommerce-backoffice-max-plugin</plugin-module>

        <!-- Hotel activities -->
        <plugin-module>modality-hotel-backoffice-max-plugin</plugin-module>

        <!-- Event activities -->
        <plugin-module>modality-event-backoffice-max-plugin</plugin-module>
        <!-- This will add the create event button in the main frame header -->
        <plugin-module>kbs-backoffice-header-festivalcreator-plugin</plugin-module>

        <!-- Catering activities -->
        <plugin-module>modality-catering-backoffice-max-plugin</plugin-module>

    </dependencies>

</project>