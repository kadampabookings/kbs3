<?xml version="1.0" encoding="UTF-8"?>
<project applicationId="org.kadampabookings.kbs.backoffice" applicationLabel="KBS">

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <providers>
        <provider interface="javafx.application.Application">org.kadampabookings.kbs.backoffice.KbsBackOfficeApplication</provider>
    </providers>

    <dependencies>
        <used-by-source-modules/>

        <!-- Allow configuration variables from environment in Java executables (OpenJFX or Gluon) -->
        <plugin-module executable-target="java">webfx-stack-conf-env-java</plugin-module>

        <!-- Base activities -->
        <plugin-module>modality-base-backoffice-activities-all</plugin-module>

        <!-- CRM activities -->

        <!-- Ecommerce activities -->
        <plugin-module>modality-ecommerce-backoffice-activities-all</plugin-module>

        <!-- Hotel activities -->
        <plugin-module>modality-hotel-backoffice-activities-all</plugin-module>

        <!-- Event activities -->

        <!-- Catering activities -->
        <plugin-module>modality-catering-backoffice-activities-all</plugin-module>

    </dependencies>

</project>