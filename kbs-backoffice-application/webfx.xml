<?xml version="1.0" encoding="UTF-8"?>
<project applicationId="org.kadampabookings.kbs.backoffice" applicationLabel="KBS">

    <description>The KBS Back-Office application (cross-platform non-executable module).</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>

        <plugin-module>kbs-client-conf</plugin-module>

        <plugin-module>modality-base-backoffice-application</plugin-module>

        <!-- Base activities -->
        <plugin-module>modality-base-backoffice-activities-all</plugin-module>

        <!-- CRM activities -->

        <!-- Ecommerce activities -->
        <plugin-module>modality-ecommerce-backoffice-activities-all</plugin-module>

        <!-- Hotel activities -->
        <plugin-module>modality-hotel-backoffice-activities-all</plugin-module>

        <!-- Event activities -->
        <plugin-module>modality-event-backoffice-activities-all</plugin-module>

        <!-- Catering activities -->
        <plugin-module>modality-catering-backoffice-activities-all</plugin-module>

    </dependencies>

</project>