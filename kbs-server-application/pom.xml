<?xml version="1.0" encoding="UTF-8"?>
<!-- File managed by WebFX (DO NOT EDIT MANUALLY) -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.kadampabookings</groupId>
        <artifactId>kbs3</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>kbs-server-application</artifactId>

    <description>The KBS Server application (cross-platform non-executable module).</description>

    <dependencies>

        <dependency>
            <groupId>org.kadampabookings</groupId>
            <artifactId>kbs-server-newsimport-plugin</artifactId>
            <version>1.0.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.kadampabookings</groupId>
            <artifactId>kbs-server-podcastsimport-plugin</artifactId>
            <version>1.0.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-base-server-max-plugin</artifactId>
            <version>1.0.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-crm-server-max-plugin</artifactId>
            <version>1.0.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-ecommerce-server-max-plugin</artifactId>
            <version>1.0.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-substitution-var-env-plugin</artifactId>
            <version>0.1.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

    </dependencies>

</project>