<?xml version="1.0" encoding="UTF-8"?>
<project applicationId="org.kadampabookings.kbs.frontoffice" applicationLabel="Kadampa Bookings" pwa="false">

    <description>The KBS Front-Office application (cross-platform non-executable module).</description>

    <exported-packages>
        <source-packages/>
        <resource-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>

        <plugin-module>kbs-client-conf-plugin</plugin-module>
        <plugin-module executable-target="gluon">kbs-client-conf-gluon-plugin</plugin-module>

        <!-- Kadampa Home -->
        <plugin-module>kbs-frontoffice-activity-home-plugin</plugin-module>

        <!-- Kadampa News -->
        <!-- Unused in production for now
        <plugin-module>kbs-frontoffice-activity-news-plugin</plugin-module>
        -->

        <!-- Kadampa Podcasts -->
        <!-- Unused in production for now
        <plugin-module>kbs-frontoffice-activity-podcasts-plugin</plugin-module>
        -->

        <!-- Kadampa Books -->
        <!-- Unused in production for now
        <plugin-module>kbs-frontoffice-activity-books-plugin</plugin-module>
        -->

        <!-- Customized Login -->
        <plugin-module>kbs-frontoffice-activity-login-plugin</plugin-module>

        <!-- Base activities -->
        <plugin-module>modality-base-frontoffice-max-plugin</plugin-module>
        <!-- <plugin-module>modality-base-client-activity-console-plugin</plugin-module> -->

        <!-- Ecommerce activities -->
        <plugin-module>modality-ecommerce-frontoffice-max-plugin</plugin-module>

        <!-- CRM activities -->
        <plugin-module>modality-crm-frontoffice-max-plugin</plugin-module>

        <!-- Event activities (booking page) -->
        <plugin-module>modality-event-frontoffice-max-plugin</plugin-module>
        <plugin-module>kbs-frontoffice-bookingform-nkt-plugin</plugin-module>
        <plugin-module>kbs-frontoffice-bookingform-mkmc-plugin</plugin-module>
        <plugin-module>kbs-frontoffice-bookingform-kmcny-plugin</plugin-module>

    </dependencies>


</project>